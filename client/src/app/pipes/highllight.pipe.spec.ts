import { HighllightPipe } from './highllight.pipe';

describe('HighllightPipe', () => {
  it('create an instance', () => {
    const pipe = new HighllightPipe();
    expect(pipe).toBeTruthy();
  });
});
